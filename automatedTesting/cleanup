parent_dir="$(dirname "$PWD")"

if [[ "$(basename $parent_dir)" != "CO_M21" ]]; then
	echo Aborting: Wrong directory. Run from CO_M21/*.
	exit 1
fi


	rm ../*/*.png > /dev/null 2> /dev/null
	rm ../*.png > /dev/null 2> /dev/null
	echo Cleared all PNGs in $parent_dir


